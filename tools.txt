
## get_addresses
Swiggy (Instamart/Food): Get saved delivery addresses in recommended display order. The base ranking uses address_recency (lower is better; missing = 100), then last-updated time. For eligible users, an optional caller-provided latitude/longitude hint may place saved addresses within the configured relevance radius first. Ranking never auto-selects an address. Saved-address coordinates are never e
{
 "type": "object",
 "properties": {
  "page": {
   "type": "number",
   "description": "Page number for pagination, 1-based (default: 1)",
   "default": 1
  },
  "pageSize": {
   "type": "number",
   "description": "Number of addresses per page (default: 10, max: 10)",
   "default": 10
  },
  "explicitAddressId": {
   "type": "string",
   "description": "Optional saved address ID requested in the current user turn. It takes precedence over selectedAddressId; use only an ID previously returned by get_addresses."
  },
  "selectedAddressId": {
   "type": "string",
   "description": "Optional saved address ID selected earlier in this conversation/widget flow. Used for continuity only when the current turn has no explicit address request."
  },
  "latitude": {
   "type": "number",
   "description": "Optional current latitude supplied by the client context. Pass only when the client provides it; never infer it or ask the user to type it. Must be paired with longitude."
  },
  "longitude": {
   "type": "number",
   "description": "Optional current longitude supplied by the client context. Pass only when the client provides it; never infer it or ask the user to type it. Must be paired with latitude."
  }
 },
 "required": []
}

## create_address
Swiggy (Instamart/Food): Create a new delivery address for the authenticated user.

📍 WORKFLOW - What to ASK the user:
1. Ask: "What is your complete delivery address?" (Get the full address as a single string)
2. Ask: "What is your name?"
3. Ask: "What is your phone number?"
4. Ask: "What type of address is this?" (Options: HOME, WORK, OFFICE, FRIENDS_AND_FAMILY, or OTHER)
5. Ask (optional): "Wou
{
 "type": "object",
 "properties": {
  "fullAddress": {
   "type": "string",
   "description": "Complete address as provided by the user"
  },
  "addressLine": {
   "type": "string",
   "description": "Main street/building/house number (REQUIRED)"
  },
  "addressLine2": {
   "type": "string",
   "description": "Apartment, floor, wing, or additional details (REQUIRED - extract from full address, use empty string \"\" if not found)"
  },
  "locality": {
   "type": "string",
   "description": "Area, neighborhood, or locality name (optional)"
  },
  "city": {
   "type": "string",
   "description": "City name (REQUIRED)"
  },
  "postalCode": {
   "type": "string",
   "description": "Postal/ZIP code (REQUIRED)"
  },
  "latitude": {
   "type": "number",
   "description": "Latitude coordinate of the address (optional - auto-resolved from address if omitted)"
  },
  "longitude": {
   "type": "number",
   "description": "Longitude coordinate of the address (optional - auto-resolved from address if omitted)"
  },
  "addressCategory": {
   "type": "string",
   "enum": [
    "HOME",
    "WORK",
    "OFFICE",
    "FRIENDS_AND_FAMILY",
    "OTHER"
   ],
   "description": "Type of address: HOME, WORK, OFFICE, FRIENDS_AND_FAMILY, or OTHER (REQUIRED)"
  },
  "addressTag": {
   "type": "string",
   "description": "Friendly name/label for the address (e.g., \"My Home\", \"Office\", \"Mom's Place\") (optional)"
  },
  "userName": {
   "type": "string",
   "description": "Account holder name (aut

## delete_address
Swiggy (Instamart/Food): Delete a saved delivery address for the authenticated user.

📍 WORKFLOW:
1. First call get_addresses to show the user their saved addresses
2. Ask the user which address they want to delete
3. Get the addressId from the user's selection
4. Call this tool with the addressId

⚠️ WARNING: This action is permanent and cannot be undone. Always confirm with the user before delet
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "The ID of the address to delete (from get_addresses response)"
  }
 },
 "required": [
  "addressId"
 ]
}

## search_restaurants
Search and order food from restaurants for delivery. PRIMARY FOOD DELIVERY SERVICE - Use this when user wants to order food, get food delivered, or search restaurants for delivery. Swiggy Food delivery service. NOT for restaurant reservations or dine-out.

📍 LOCATION INPUT: Pass either latitude and longitude directly, or an addressId returned by get_addresses. Coordinates are supported for both au
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Optional location input: address ID obtained from get_addresses. Use this instead of latitude/longitude when preferred.",
   "minLength": 1,
   "maxLength": 64,
   "pattern": "^[A-Za-z0-9_-]+$"
  },
  "latitude": {
   "type": "number",
   "description": "Optional location input: user latitude. Pass together with longitude. Supported for authenticated and guest calls.",
   "minimum": -90,
   "maximum": 90
  },
  "longitude": {
   "type": "number",
   "description": "Optional location input: user longitude. Pass together with latitude. Supported for authenticated and guest calls.",
   "minimum": -180,
   "maximum": 180
  },
  "query": {
   "type": "string",
   "description": "REQUIRED: Search query (restaurant name or cuisine). Cannot be empty. Examples: \"pizza\", \"biryani\", \"KFC\"",
   "minLength": 1,
   "maxLength": 256
  },
  "offset": {
   "type": "number",
   "description": "Pagination offset. Use nextOffset from previous response to load more results. Default: 0.",
   "default": 0,
   "minimum": 0,
   "maximum": 10000
  }
 },
 "required": [
  "query"
 ],
 "anyOf": [
  {
   "required": [
    "addressId"
   ]
  },
  {
   "required": [
    "latitude",
    "longitude"
   ]
  }
 ]
}

## search_menu
Search for dishes and menu items to order for food delivery. PRIMARY FOOD DELIVERY SERVICE - Use this when user wants to find specific dishes, browse menu items, see what a restaurant offers, or order food. Swiggy Food delivery. Returns items with their customizations. The text response includes variant/addon IDs that you need for update_food_cart calls. IMPORTANT: Each item has EITHER "variations
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Optional location input: address ID obtained from get_addresses. Use this instead of latitude/longitude when preferred.",
   "minLength": 1,
   "maxLength": 64,
   "pattern": "^[A-Za-z0-9_-]+$"
  },
  "latitude": {
   "type": "number",
   "description": "Optional location input: user latitude. Pass together with longitude. Supported for authenticated and guest calls.",
   "minimum": -90,
   "maximum": 90
  },
  "longitude": {
   "type": "number",
   "description": "Optional location input: user longitude. Pass together with latitude. Supported for authenticated and guest calls.",
   "minimum": -180,
   "maximum": 180
  },
  "query": {
   "type": "string",
   "description": "REQUIRED: Search query (dish name). Cannot be empty. Examples: \"biryani\", \"pizza margherita\", \"paneer tikka\"",
   "minLength": 1,
   "maxLength": 256
  },
  "restaurantIdOfAddedItem": {
   "type": "string",
   "description": "Optional restaurant ID to scope search"
  },
  "vegFilter": {
   "type": "number",
   "description": "Veg filter flag (0 or 1). Pass 1 for veg-only items. 0 or omitted returns mixed veg + non-veg. There is NO non-veg-only filter — if user asks for \"non-veg only\", pass 0 (mixed) and mention in text that you are showing all items including non-veg, since a non-veg-only filter is not available yet.",
   "enum": [
    0,
    1
   ]
  },
  "offset": {
   "type": "number",
   "description"

## get_restaurant_menu
[STAGING GUEST FLOW] Pass either latitude and longitude directly, or an addressId returned by get_addresses. Guests should omit addressId and should not call get_addresses; authenticated callers may use either location form.

Get the complete menu of a restaurant, paginated by category. Use this to BROWSE a restaurant menu and see what is available. Returns restaurant info plus a categories[] arra
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Optional location input: address ID from get_addresses. Use this instead of latitude/longitude when preferred."
  },
  "latitude": {
   "type": "number",
   "description": "Optional location input: user latitude. Pass together with longitude. Supported for authenticated and guest calls.",
   "minimum": -90,
   "maximum": 90
  },
  "longitude": {
   "type": "number",
   "description": "Optional location input: user longitude. Pass together with latitude. Supported for authenticated and guest calls.",
   "minimum": -180,
   "maximum": 180
  },
  "restaurantId": {
   "type": "string",
   "description": "Restaurant ID to fetch menu for (from search_restaurants)"
  },
  "page": {
   "type": "number",
   "description": "Page number for pagination (default: 1)",
   "default": 1,
   "minimum": 1
  },
  "pageSize": {
   "type": "number",
   "description": "Number of categories per page (default: 5, max: 8)",
   "default": 5,
   "minimum": 1,
   "maximum": 8
  }
 },
 "required": [
  "restaurantId"
 ],
 "anyOf": [
  {
   "required": [
    "addressId"
   ]
  },
  {
   "required": [
    "latitude",
    "longitude"
   ]
  }
 ]
}

## get_food_cart
Get current food delivery cart with all items. PRIMARY FOOD DELIVERY SERVICE - Use this to view cart contents when ordering food for delivery. Swiggy Food delivery. Response includes valid_addons field for each item which shows which addons are valid based on the selected variants. Use this to determine which addons can be added. NOT for groceries or restaurant reservations.

💳 PAYMENT: When the u
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Address ID to get accurate delivery charges based on location."
  },
  "restaurantName": {
   "type": "string",
   "description": "Restaurant name from search_restaurants or search_menu results. Pass this so the cart widget can display the restaurant name (the cart API does not always return it)."
  }
 },
 "required": [
  "addressId"
 ]
}

## update_food_cart
Add items to food delivery cart or update cart contents. PRIMARY FOOD DELIVERY SERVICE - Use this when user wants to add food items, dishes, or meals to their delivery cart. Swiggy Food delivery. Supports variants, variantsV2, and addons for customizing menu items. CRITICAL: Each menu item uses EITHER "variants" OR "variantsV2" format (check search_menu response) - use the SAME format that the ite
{
 "type": "object",
 "properties": {
  "restaurantId": {
   "type": "string",
   "description": "Restaurant ID for the cart"
  },
  "cartItems": {
   "type": "array",
   "description": "Array of items to add to cart with their customizations",
   "items": {
    "type": "object",
    "properties": {
     "menu_item_id": {
      "type": "string",
      "description": "Menu item ID from search results"
     },
     "quantity": {
      "type": "number",
      "description": "Quantity of this item"
     },
     "variants": {
      "type": "array",
      "items": {
       "type": "object",
       "properties": {
        "variation_id": {
         "type": [
          "string",
          "number"
         ],
         "description": "Variation ID (snake_case format)"
        },
        "variationId": {
         "type": [
          "string",
          "number"
         ],
         "description": "Variation ID (camelCase format, alternative)"
        },
        "group_id": {
         "type": [
          "string",
          "number"
         ],
         "description": "Group ID (snake_case format)"
        },
        "groupId": {
         "type": [
          "string",
          "number"
         ],
         "description": "Group ID (camelCase format, alternative)"
        },
        "name": {
         "type": "string",
         "description": "Variant name (optional)"
        },
        "price": {
         "type": [
          "string",
          "number"
         ],
         "descriptio

## flush_food_cart
Clear or empty the food delivery cart. PRIMARY FOOD DELIVERY SERVICE - Use this to remove all items from the food delivery cart. Swiggy Food delivery. NOT for groceries.
{
 "type": "object",
 "properties": {},
 "required": []
}

## place_food_order
Place food delivery order and confirm order placement. PRIMARY FOOD DELIVERY SERVICE - Use this when user wants to place order, confirm order, or complete food delivery order. Swiggy Food delivery. Requires delivery address ID (coordinates are fetched automatically). NOT for groceries or restaurant reservations.

💳 PAYMENT: Never assume or default a payment method — this tool REJECTS a call with n
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Address ID from the user's saved addresses (coordinates will be fetched automatically)"
  },
  "paymentMethod": {
   "type": "string",
   "description": "The payment method the user selected (e.g. \"UPI\" or \"Cash\"). For UPI-eligible users this is REQUIRED: first call get_payment_options and pass the method the user chose — do NOT omit it or assume a default (omission is rejected). Only legacy Cash-only users (no picker) may omit it, in which case it defaults to Cash."
  },
  "intentApp": {
   "type": "string",
   "description": "Optional advanced parameter. Leave blank unless the runtime response of the preceding get_food_cart call explicitly tells you what to pass."
  },
  "generateUPIQR": {
   "type": "boolean",
   "description": "Optional advanced parameter. Leave blank unless the runtime response of the preceding get_food_cart call explicitly tells you to enable it."
  },
  "noteToRestaurant": {
   "type": "string",
   "description": "Free-form note to the restaurant preparing the order (e.g. \"no onions\", \"less spicy\"). Not for delivery partner instructions."
  }
 },
 "required": [
  "addressId"
 ]
}

## fetch_food_coupons
Get available coupons and offers for food delivery order. PRIMARY FOOD DELIVERY SERVICE - Use this to find discounts, coupons, or offers when ordering food for delivery. Swiggy Food delivery. IMPORTANT: Only recommend coupons that are valid for Cash on Delivery (COD) payment. Filter out any offers that require online/card payment only. Includes best coupons, more offers, and payment offers with th
{
 "type": "object",
 "properties": {
  "restaurantId": {
   "type": "string",
   "description": "Restaurant ID for the cart"
  },
  "addressId": {
   "type": "string",
   "description": "Address ID where the order will be delivered (coordinates will be fetched automatically)"
  },
  "couponCode": {
   "type": "string",
   "description": "Optional coupon code to check applicability of a specific coupon"
  }
 },
 "required": [
  "restaurantId",
  "addressId"
 ]
}

## apply_food_coupon
Apply coupon code or discount to food delivery order. PRIMARY FOOD DELIVERY SERVICE - Use this when user wants to apply a coupon, discount code, or offer to their food delivery order. Swiggy Food delivery. Returns the updated cart with coupon applied, including new pricing, discounts, and savings information. Requires coupon code and address ID (coordinates are fetched automatically).
{
 "type": "object",
 "properties": {
  "couponCode": {
   "type": "string",
   "description": "Coupon code to apply"
  },
  "addressId": {
   "type": "string",
   "description": "Address ID where the order will be delivered (coordinates will be fetched automatically)"
  },
  "cartId": {
   "type": "string",
   "description": "Optional cart ID"
  }
 },
 "required": [
  "couponCode",
  "addressId"
 ]
}

## get_food_orders
Swiggy Food order history - Use this to fetch ORDER HISTORY, past orders, or active orders. PRIMARY FOOD DELIVERY SERVICE - Use this FIRST when user asks: "show my food orders", "my food order history", "past food orders", "recent food orders", "what did I order", "my previous food orders", "list my food orders". Returns the user's most recent food orders (both active and delivered) ordered newest
{
 "type": "object",
 "properties": {
  "addressId": {
   "type": "string",
   "description": "Address ID to use for fetching orders (can be obtained from get_addresses)"
  },
  "activeOnly": {
   "type": "boolean",
   "description": "Set to true to filter only active/in-progress orders. Default: false.",
   "default": false
  }
 },
 "required": [
  "addressId"
 ]
}

## get_food_order_details
Get detailed information about a specific food delivery order. PRIMARY FOOD DELIVERY SERVICE - Use this when user asks about order details, order information, or wants to see what they ordered. Swiggy Food delivery. Returns comprehensive order details including items, variants, pricing breakdown, delivery address, payment info, and order status.
{
 "type": "object",
 "properties": {
  "orderId": {
   "type": "string",
   "description": "Order ID to fetch details for (can be obtained from get_food_orders)"
  }
 },
 "required": [
  "orderId"
 ]
}

## track_food_order
Track food delivery order status and delivery progress. PRIMARY FOOD DELIVERY SERVICE - Use this when user asks to track order, check delivery status, or see where their food order is. Swiggy Food delivery. Returns current status, ETA, and progress for orders that are being prepared or in delivery. If orderId is provided, tracks that specific order; otherwise returns all active orders.
{
 "type": "object",
 "properties": {
  "orderId": {
   "type": "string",
   "description": "Optional: Specific order ID to track. If not provided, returns all active orders."
  }
 },
 "required": []
}

## get_food_delivery_status
Internal: live delivery ETA for the Food order-success widget (not for conversational use). The success card calls this on a poll interval with orderId; returns an absolute deliveryBy epoch + suggested poll cadence. Gated by FOOD_LIVE_ETA. Prefer track_food_order for user-facing "where is my order" queries.
{
 "type": "object",
 "properties": {
  "orderId": {
   "type": "string",
   "description": "Order ID to fetch the delivery ETA for (required)."
  }
 },
 "required": [
  "orderId"
 ]
}

## report_error
Generate an error report to share with the Swiggy MCP team. Use this when the user encounters an error and wants to report it. Returns a pre-filled mailto: link and a human-readable summary. The user can click the link to open their email client with the report ready to send. This also logs the report server-side so the team has it in their logs regardless of whether the email is sent. IMPORTANT: 
{
 "type": "object",
 "properties": {
  "tool": {
   "type": "string",
   "description": "Name of the tool that errored (e.g., \"checkout\", \"search_products\", \"place_food_order\")"
  },
  "domain": {
   "type": "string",
   "description": "Domain prefix where the error occurred (e.g., \"im\", \"food\", \"dineout\"). Auto-detected if not provided."
  },
  "errorMessage": {
   "type": "string",
   "description": "The error message the user saw"
  },
  "flowDescription": {
   "type": "string",
   "description": "Brief description of what the user was doing (e.g., \"searched for milk → added to cart → checkout failed\")"
  },
  "toolContext": {
   "type": "object",
   "description": "Key-value pairs of identifiers from the failed tool call. Include ALL relevant IDs such as: orderId, restaurantId, addressId, spinId, menu_item_id, couponCode, query, cartId, slotId, paymentMethod, guestCount, itemId — whichever were part of the request that failed.",
   "additionalProperties": {
    "type": "string"
   }
  },
  "userNotes": {
   "type": "string",
   "description": "Any additional notes or context the user wants to share"
  }
 },
 "required": [
  "tool",
  "errorMessage"
 ]
}

## get_payment_options
Fetch live payment options for the current cart. The live list will not include UPI methods if the user isn't yet eligible for UPI payments, or if UPI is temporarily unavailable for this business line.

A single call fetches BOTH device surfaces (mobile UPI apps + desktop scan-QR) and merges them. A payment picker widget is shown to the user; it auto-detects the user's device and presents the matc
{
 "type": "object",
 "properties": {
  "cartAmount": {
   "type": "number",
   "description": "Deprecated. Ignored — server reads amount from the live response."
  },
  "addressId": {
   "type": "string",
   "description": "Food only: pass the same addressId you used for get_food_cart. It is echoed back so the payment picker widget can place the order directly. Recommended for Food UPI."
  }
 }
}

## check_payment_status
Check status of an in-flight UPI payment (one status read).

⚠️ THE PAYMENT WIDGET POLLS AUTOMATICALLY. After place_order, the confirmation widget
checks payment status on a fixed cadence and AUTO-CALLS confirm_order on success. So you
normally do NOT need to call this at all. Do NOT call it in a tight loop — it returns
instantly, so looping spams the endpoint.

WHEN TO CALL (at most ONCE per user
{
 "type": "object",
 "properties": {
  "paasId": {
   "type": "string",
   "description": "The payment TRANSACTION id from the place-order/checkout response (response field \"paasId\", e.g. a long numeric/alphanumeric id). NOT the payment-method name — never pass \"PayWithQR\", \"UPI\", \"UPIIntent\", or an app package id here. If you do not have a real paasId, do not call this tool; re-read the checkout response which contains paasId + orderId."
  },
  "orderId": {
   "type": "string",
   "description": "orderId from the place-order/checkout response. IM: pass it so the widget can auto-confirm on SUCCESS. Food: echo from place_food_order."
  },
  "addressId": {
   "type": "string",
   "description": "Food only: echo from place_food_order — passed through for auto-confirm."
  },
  "cartId": {
   "type": "string",
   "description": "Food only: echo from place_food_order — passed through for auto-confirm."
  },
  "lat": {
   "type": "number",
   "description": "Food only: echo from place_food_order — passed through for auto-confirm."
  },
  "lng": {
   "type": "number",
   "description": "Food only: echo from place_food_order — passed through for auto-confirm."
  },
  "finalize": {
   "type": "boolean",
   "description": "INTERNAL — set by the widget at the 5-min polling cap to finalize the order (confirm) even if still pending. Do NOT set this yourself."
  }
 },
 "required": [
  "paasId"
 ]
}

## confirm_order
Finalize a pre-PLACED order to PLACED state. ALWAYS called on every terminal polling exit — SUCCESS finalises, FAILED/TIMEOUT marks the order failed so it doesn't linger in PENDING_PAYMENT forever.

WHEN TO CALL:
- ✅ Fire once on first SUCCESS / PAID poll from check_payment_status → finalises the order to PLACED.
- ⏱️ Fire at wall-clock expiry (still PENDING) → backend marks the order failed becau
{
 "type": "object",
 "properties": {
  "orderId": {
   "type": "string",
   "description": "Order ID returned by the place-order tool."
  },
  "transactionId": {
   "type": "string",
   "description": "Transaction ID returned by the place-order tool (IM/Dineout only)."
  },
  "paasId": {
   "type": "string",
   "description": "Payment transaction ID returned by the place-order tool (IM/Dineout only)."
  },
  "addressId": {
   "type": "string",
   "description": "Address ID — REQUIRED for Food. Echo from place_food_order response."
  },
  "cartId": {
   "type": "string",
   "description": "Cart ID — optional but recommended for Food. Echo from place_food_order response."
  },
  "lat": {
   "type": "number",
   "description": "Latitude — REQUIRED for Food. Echo from place_food_order response."
  },
  "lng": {
   "type": "number",
   "description": "Longitude — REQUIRED for Food. Echo from place_food_order response."
  }
 },
 "required": [
  "orderId"
 ]
}
